#ifndef ARRAYS_H
#define ARRAYS_H

#include <stddef.h>


double average(int array[], size_t dim);
void initialize_range(int array[], size_t dim);
int min(int array[], size_t dim);
int max(int array[], size_t dim);
long product(int array[], size_t dim);
int sum(int array[], size_t dim);

#endif  // ARRAYS_H
